{{setTitle 'Create Page'}}


<!-- In case of error, you should display div with class "errorContainer" -->
<!-- You can choose to display the first error or all of them (error message/s in your opinion) -->

<!-- <div>
                <div class="errorContainer">
                    <p>Error</p>
                </div>
            </div> -->

<section id="create-container">
    <div class="create-container-info">
        <h1>Create Event</h1>
        <form method="POST">
            <label>Name of the Disaster:</label>
            <input type="text" id="name" name="name" placeholder="..." value="{{userInput.name}}">
            <label>Type of Disaster:</label>
            <select id="type" name="type">
                {{#each options}}
                <option {{#if selected}}selected="{{selected}}" {{/if}} value="{{value}}">{{content}}</option>
                {{/each}}
            </select>
            <label>Year of the Event:</label>
            <input type="number" id="year" name="year" placeholder="..." value="{{userInput.year}}">
            <label>Location:</label>
            <input type="text" id="location" name="location" placeholder="..." value="{{userInput.location}}">
            <label>Image:</label>
            <input type="text" id="image" name="image" placeholder="http://..." value="{{userInput.image}}">
            <label>Description:</label>
            <textarea id="description" name="description" placeholder="...">{{userInput.description}}</textarea>
            <input type="submit" id="btn" value="Submit"></input>
        </form>
    </div>
</section>